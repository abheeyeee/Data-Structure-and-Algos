#include<bits/stdc++.h>
using namespace std;

int main(){
    int maxi = *max_element(a,a+n); // this gives u the adddress of the max or mini and using * gives the element.
    int mini = *min_element(a,a+n);
}