#include <bits/stdc++.h>
using namespace std;  

void explainMultiMap(){
    // everything same as map, only diff is it can store multiple duplicate keys
    // only mpp[key] cannot be used here
}

int main(){
    explainMultiMap();
}