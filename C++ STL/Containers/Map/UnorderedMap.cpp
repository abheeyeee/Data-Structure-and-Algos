#include <bits/stdc++.h>
using namespace std;  

void explainUnorderedMap(){ // unique and unsorted
    // Duplicate keys are not allowed → inserting a key that exists overwrites the value.
     // same as set and unordered_set difference.
     // not in sorted order
     // not have duplicate keys
     // Print elements (order is not guaranteed)

     //Average time complexity for insertion, deletion, lookup = O(1).
     //Worst case = O(n) (if many collisions).
}

int main(){
    explainUnorderedMap();
}