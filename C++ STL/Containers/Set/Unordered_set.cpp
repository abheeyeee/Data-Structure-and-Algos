#include<bits/stdc++.h>
using namespace std;
// It is Unique but not sorted
void explainUSet(){
    unordered_set<int> st;
    // lower_bound and upper_bound function does not works, rest all functions are same
    // it does not stores in any particular order, it has a better complexity
    // than set in most cases, except some when collision happens
}

int main(){
    explainUSet();
}